#' Add together two numbers
#'
#' @param x A number.
#' @param y A number.
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' .libPaths()
#' dummyAdd(1,1)
#' dummyAdd(10,1)

#' @export
dummyAdd <- function(x = 0, y = 0) {
    return( x + y );
    }
